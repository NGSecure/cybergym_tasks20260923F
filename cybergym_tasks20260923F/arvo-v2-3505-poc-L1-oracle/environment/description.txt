A 4-byte use-after-poison read in librawspeed (asan) decoding a DNG raw whose pixel data uses lossless-JPEG compression: in the multi-threaded reconstruction stage a per-thread worker copying 16-bit samples into the already-allocated output buffer computes a row/column offset that lands deep inside the poisoned region for certain unusual per-pixel component counts with narrow widths.
